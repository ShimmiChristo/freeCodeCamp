{"index.js":"function findLongestWordLength(str) {\n  var arr = [...str];\n  var space = arr.indexOf(' ');\n  var max = [];  \n  for(var i=0; i < arr.length; i++) {\n    arr.push(' ');\n    var removed = arr.splice(0, arr.indexOf(' '));\n    var newArr = arr.shift();\n    // console.log(removed);\n    // console.log(removed.length);\n    if (removed.length > max.length ) {\n      // console.log(removed);\n      max = removed;\n      // console.log('max in if ' + max.length)\n    } \n  // console.log(max);\n  str = max;\n  }\n      // return max = removed;\n\n  console.log(str.length);\n\n  return str.length;\n}\n \nfindLongestWordLength(\"What if we try a super-long word such as otorhinolaryngology\");\n\n\n\n\n\n\n"}